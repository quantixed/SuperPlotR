# Ensure working directory setup

Checks if the directories ("Data", "Output", "Output/Data",
"Output/Plots", "Script") exist in the current working directory. If any
of these directories are missing, it creates them to ensure that the
project structure is properly set up.

## Usage

``` r
setup_rproj()
```

## Value

No return value
